# Logo Setup Instructions

## Logo Status: ✅ Temporary SVG Created

I've created a temporary SVG logo that displays now. To use your actual logo:

### Option 1: Replace with PNG (Recommended)
1. Save your Vertex logo image as: `public/vertex-logo.png`
2. Update these files to use `.png` instead of `.svg`:
   - `src/components/Navbar.jsx` (line with logo-img)
   - `src/components/Hero.jsx` (line with hero-logo)
   - `src/components/Footer.jsx` (line with footer logo)

### Option 2: Use SVG
Save your logo as `public/vertex-logo.svg` and it will automatically replace the temporary one.

### Current Logo Locations:
1. **Navbar** - Top left corner (50px → 40px when scrolled)
2. **Hero Section** - Center top (80px height)
3. **Footer** - Center bottom (60px height)

---

## ✅ Contact Section Fixed
Phone number and email are now visible with dark text (#1a1a1a) instead of white.

